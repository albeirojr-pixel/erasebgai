{
  "name": "erasebgai",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  }
}
```

Guarda con **Ctrl + S**, luego en PowerShell:
```
git add .
git commit -m "Add package.json for Node 18"
git push origin main